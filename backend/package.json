{
  "name": "flatshare-ledger-backend",
  "version": "1.0.0",
  "description": "Backend for the Flatshare Ledger expense-splitting app",
  "main": "src/server.js",
  "type": "commonjs",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "node src/server.js",
    "start": "node src/server.js",
    "import": "node src/importer/runImport.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "test:import": "node src/importer/runImport.js --dry-run"
  },
  "dependencies": {
    "@prisma/client": "^6.9.0",
    "cors": "^2.8.5",
    "csv-parse": "^5.6.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "prisma": "^6.9.0"
  }
}
